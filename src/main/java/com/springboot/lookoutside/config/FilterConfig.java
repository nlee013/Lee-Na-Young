package com.springboot.lookoutside.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FilterConfig {
/*
	@Bean
	public FilterRegistrationBean<MyFilter1> filter1() {
		FilterRegistrationBean<MyFilter1> bean = new FilterRegistrationBean<MyFilter1>(new MyFilter1());
		bean.addUrlPatterns("/*"); //모든 요청에서 필터를 거치게 함.
		bean.setOrder(0); //낮은 번호가 필터중에서 가장 먼저 실행된다. 단 시큐리티 필터가 제일먼저다.
		return bean;
	}
	*/
}
